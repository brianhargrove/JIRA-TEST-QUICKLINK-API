'use strict';

module.exports.wireup = function(injector, configName) {
  var wireup = this.dir(__dirname);

  // wireup your sub library modules here:
  //
  // wireup('./yourmodule');
  // wireup('./yourothermodule');
};
